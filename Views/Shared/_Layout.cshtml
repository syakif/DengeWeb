<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]  Denge Defence</title>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] | Denge Defence</title>
    
    <!-- FAVICON -->
    <link rel="icon" type="image/png" href="/images/defence-Photoroom-1536x956.png">

    <!-- AOS (Animate On Scroll) -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- FontAwesome (Sosyal Medya İkonları İçin) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* Tasarıma Özel Koyu Mavi Degrade Arka Plan */
        .custom-footer {
    background: linear-gradient(135deg, #010846 0%, #023080 100%);
    color: #ffffff;
    padding-top: 2rem;
    padding-bottom: 0.8rem;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
}

.custom-footer .row {
    margin-bottom: 0;
}

.custom-footer h6 {
    margin-bottom: 0.8rem !important;
}

.custom-footer ul {
    margin-bottom: 0;
}

.custom-footer li {
    margin-bottom: 0.4rem !important;
}

.custom-footer .mt-4,
.custom-footer .mt-md-5,
.custom-footer .pt-3 {
    margin-top: 0 !important;
    padding-top: 0 !important;
}

.footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding: 0.9rem 0;
    margin-top: 1rem;
}

.custom-footer a,
.custom-footer a:visited,
.custom-footer a:active {
    color: #d2d2d2 !important;
    text-decoration: none;
}

.custom-footer a:hover {
    color: #a4a4a5 !important;
    text-decoration: none;
}

        /* Sosyal Medya Yuvarlak Buton Tasarımı */
        .social-icons a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.08);
            color: #ffffff;
            margin-left: 0.5rem;
            transition: background 0.3s ease;
        }

        .social-icons a:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .dng-logo {
            font-size: 1.8rem;
            letter-spacing: 2px;
        }
        
        .dng-highlight {
            background-color: #0d6efd;
            padding: 0 5px;
            border-radius: 3px;
        }

        /* Görseller için özel lacivert çerçeve */
        .navy-frame {
            border: 2px solid #001f54; /* 2 piksel kalınlığında lacivert çizgi */
            padding: 3px; /* Çerçeve ile resim arasında ince, şık bir beyaz boşluk bırakır */
            background-color: #ffffff;
        }

        /* Başlangıçtaki Şeffaf Menü (Sadece Anasayfada) */
.navbar-transparent {
    background-color: transparent !important;
    box-shadow: none !important;
    transition: all 0.4s ease-in-out;
    padding-top: 1px;
    padding-bottom: 1px;
}

/* Sayfa Kaydırıldığında Gelecek Lacivert Menü */
.navbar-scrolled {
    background: linear-gradient(135deg, #ffffff 0%, #ffffff 100%) !important;
    box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.4) !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    transition: all 0.4s ease-in-out;
    padding-top: 3px;
    padding-bottom: 3px;
}

        /* Üst Menü Logo Ayarları */
        .navbar-brand img {
            height: 120px; /* Logonun yüksekliği, duruma göre artırıp azaltabilirsin */
            margin-right: 12px; /* Logo ile yazı arasındaki boşluk */
            object-fit: contain;
        }

        /* 1. Şeffaf Menüdeki Link Rengi (Video üzerindeyken: Hafif Gri / Gümüş) */
.navbar-transparent .nav-link {
    color: #d3d7dc !important; /* Açık gri tonu */
    transition: color 0.3s ease-in-out;
}
/* Şeffaf menüde linkin üzerine gelince (Hover) bembeyaz olsun */
.navbar-transparent .nav-link:hover {
    color: #ffffff !important; 
}

/* 2. Lacivert Menüdeki Link Rengi (Kaydırıldığında: Tam Beyaz veya farklı bir renk) */
/* Lacivert arka planda yüksek kontrast için bembeyaz veya gümüş olması idealdir */
.navbar-scrolled .nav-link {
    color: #12005c !important; /* Lacivert üstünde net beyaz */
    transition: color 0.3s ease-in-out;
}
/* Lacivert menüde linkin üzerine gelince logodaki gümüş/gri tona dönsün */
.navbar-scrolled .nav-link:hover {
    color: #2100fb !important; 
}

/* Footer Ana Kasa - KESİN BEYAZ ARKA PLAN */
.custom-footer {
    background: #001f54 !important; /* Eski lacivert degradeleri ezer, kesin beyaz yapar */
    position: relative;
    overflow: hidden; 
    border-top: 1px solid #eaeaea; 
    padding-top: 4rem;
    padding-bottom: 1rem;
}

/* Sol Kenardaki Lacivert Nokta Bulutu */
.custom-footer::before {
    content: '';
    position: absolute;
    left: -50px;
    bottom: -50px;
    width: 350px;
    height: 350px;
    background-image: radial-gradient(#ffffff 2px, transparent 2px); /* Lacivert Noktalar */
    background-size: 20px 20px; 
    opacity: 0.3; 
    z-index: 0;
    -webkit-mask-image: radial-gradient(circle at bottom left, black, transparent 60%);
    mask-image: radial-gradient(circle at bottom left, black, transparent 60%);
}

/* Sağ Kenardaki Lacivert Nokta Bulutu */
.custom-footer::after {
    content: '';
    position: absolute;
    right: -50px;
    bottom: -50px;
    width: 350px;
    height: 350px;
    background-image: radial-gradient(#ffffff 2px, transparent 2px); /* Lacivert Noktalar */
    background-size: 20px 20px;
    opacity: 0.3;
    z-index: 0;
    -webkit-mask-image: radial-gradient(circle at bottom right, black, transparent 60%);
    mask-image: radial-gradient(circle at bottom right, black, transparent 60%);
}

/* Footer İçeriği (Noktaların Üstünde Kalması İçin) */
.custom-footer .container {
    position: relative;
    z-index: 1;
}

/* Sosyal Medya İkonları Hover Efekti */
.footer-socials a {
    color: #001f54; /* Lacivert */
    font-size: 1.3rem;
    margin-right: 18px;
    transition: color 0.3s;
}
.footer-socials a:hover {
    color: #a3a7ab; 
}

        /* Metalik Yazı Efekti */
.metallic-text {
    font-family: "Arial Black", "Impact", sans-serif; /* Keskin, kalın ve köşeli bir font */
    font-style: italic; /* Logodaki gibi yatık (italik) görünüm */
    font-weight: 900;
    font-size: 1.6rem;
    letter-spacing: 2px;
    margin-bottom: 0;

    /* Gümüş / Çelik Yansıma Efekti (Degrade) */
    background: linear-gradient(
        to bottom,
        #f8f9fa 0%,    /* En üstteki parlak ışık */
        #c2c2c3 35%,   /* Ortaya doğru koyulaşan gri */
        #c1c2c2 50%,   /* Logodaki keskin koyu çukur çizgisi */
        #c3c3c3 65%,   /* Tekrar aydınlanan alt kısım */
        #d1d5d8 100%   /* En alt metalik yansıma */
    );
    
    /* Arka planı yazının içine hapsetme */
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: transparent; /* Yedek olarak */
    
    /* Yazının logodaki gibi 3 boyutlu durması için alt gölge (drop-shadow) */
    filter: drop-shadow(2px 3px 3px rgba(0, 0, 0, 0.8));
}

#mainNavbar .navbar-nav .nav-link {
    font-size: 1.15rem !important; /* Yazıları hafifçe büyüttük (Varsayılan genelde 1rem'dir) */
    padding-left: 1.5rem !important; /* Linklerin solundaki boşluğu artırdık */
    padding-right: 1.5rem !important; /* Linklerin sağındaki boşluğu artırdık */
}

/* Ürün Slider Sabit Yükseklik Ayarı */
.fixed-slider {
    height: 520px; /* Yüksekliği biraz daha artırdık ve kilitledik */
    overflow: hidden; /* EN ÖNEMLİSİ: Kutunun dışına taşmaya çalışan her şeyi çerçevenin içinde tutar */
}
.fixed-slider .carousel-item {
    height: 100%;
}
/* Mobildeki ayarı da ufakça güncelleyelim */
@@media (max-width: 768px) {
    .fixed-slider {
        height: auto;
        min-height: 560px; 
    }
}
    </style>
</head>
<!-- Sayfanın her zaman tam ekran yüksekliğinde olmasını sağlayan flex sınıfları -->
<body class="d-flex flex-column min-vh-100">
    <header>
        @{
            // Eğer sayfa anasayfa ise menü şeffaf ve sayfanın üstüne yapışık (fixed) olacak.
            // Diğer sayfalarda ise normal lacivert (scrolled) ve yukarı yapışkan (sticky) olacak.
            bool isHome = ViewData["IsHome"] != null && (bool)ViewData["IsHome"];
            string navClasses = isHome ? "fixed-top navbar-transparent" : "sticky-top navbar-scrolled";
        }
        <!-- Navbar'a id ekledik ve sınıfları dinamikleştirdik -->
        <nav id="mainNavbar" class="navbar navbar-expand-lg navbar-dark @navClasses">
            <div class="container">
                <!-- Logo ve Yazı -->
                <a class="navbar-brand fw-bold d-flex align-items-center" asp-controller="Home" asp-action="Index">
                    <!-- 1. BEYAZ LOGO (Anasayfa açıldığında görünecek) -->
                    <!-- src kısmına elindeki BEYAZ logo dosyasının adını yaz -->
                    <img id="logoWhite" src="/images/ddlogonobg.png" alt="Denge Defence Logo" style="height: 100px; margin-right: 0px; object-fit: contain; transition: opacity 0.3s;">
            
                    <!-- 2. RENKLİ LOGO (Menü lacivert olunca görünecek - Başlangıçta gizli (d-none)) -->
                    <!-- src kısmına elindeki RENKLİ logo dosyasının adını yaz -->
                    <img id="logoColor" src="/images/defence-Photoroom-1536x956.png" alt="Denge Defence Logo" class="d-none" style="height: 100px; margin-right: 0px; object-fit: contain; transition: opacity 0.3s;">
            
                    <!--<span class="text-white">DENGE DEFENCE</span>-->
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Index">Anasayfa</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="About">Hakkımızda</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Products">Ürünlerimiz</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Contact">İletişim</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- Ana İçerik Alanı -->
    <!-- YENİ HALİ (BUNU YAPIŞTIR): -->
    @if (ViewData["IsHome"] != null && (bool)ViewData["IsHome"])
    {
        <!-- Eğer sayfa anasayfaysa, hiçbir kısıtlama olmadan tam ekran çalışsın -->
        <main role="main">
            @RenderBody()
        </main>
    }
    else
    {
        <!-- Diğer sayfalar (Hakkımızda, İletişim vb.) standart kutu içinde kalsın -->
        <!-- mt-5 pt-5 verdik ki üstteki lacivert menünün altında ezilmesinler -->
        <div class="container mt-5 pt-5">
            <main role="main" class="pb-3">
                @RenderBody()
            </main>
        </div>
    }

    <!-- Footer Başlangıcı -->
    <footer class="custom-footer">
        <div class="container">
            <div class="row mb-5">
                
                <!-- 1. Sütun: Logo -->
                <div class="col-md-4 mb-4 mb-md-0">
                    <div class="d-flex align-items-center mb-2">
                        <img src="/images/ddlogonobg.png" alt="Denge Defence Logo" style="height: 120px; margin-right: 15px; object-fit: contain;">
                    </div>
                </div>

                <!-- 2. Sütun: Ofis -->
                <div class="col-md-4 mb-4 mb-md-0">
                    <h5 class="fw-bold mb-4" style="color: #ffffff;">İletişim</h5>
                    <ul class="list-unstyled text-white" style="line-height: 1.8; font-size: 0.95rem;">
                        <li>Küçükbakkalköy Mah. Derebey Sk. No: 6 34750</li>
                        <li>Ataşehir / İstanbul</li>
                        <li>0216 573 40 40</li>
                        <li class="mt-4">
                            <a href="mailto:info@dengedefence.com.tr" class="text-muted text-decoration-none border-bottom border-secondary pb-1">
                                info@dengedefence.com.tr
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- 3. Sütun: Sosyal Medya -->
                <div class="col-md-4">
                    <h5 class="fw-bold mb-4" style="color: #ffffff;">Sosyal</h5>
                    <div class="footer-socials">
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-x-twitter"></i></a>
                        <a href="#"><i class="fab fa-linkedin-in"></i></a>
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                
            </div>

            <!-- Alt Bar: Sadece Telif Hakkı (Yukarı Çık butonu kaldırıldı) -->
            <div class="row pt-4 border-top" style="border-color: #eaeaea !important;">
                <div class="col-12 text-center text-md-start">
                    <p class="text-white mb-0" style="font-size: 0.85rem;">
                        &copy; Copyright @DateTime.Now.Year Denge Defence. All rights reserved.
                    </p>
                </div>
            </div>
            <!--5414027382-->
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        // Animasyonları başlatıyoruz
        AOS.init({
            once: true, // Animasyon sadece bir kere çalışsın (sürekli inip çıkarken tekrarlamasın)
            offset: 100 // Öğe ekrana 100px girince animasyon başlasın
        });
    </script>

    <script>
    var navbar = document.getElementById("mainNavbar");
    var logoWhite = document.getElementById("logoWhite"); // Beyaz logoyu yakaladık
    var logoColor = document.getElementById("logoColor"); // Renkli logoyu yakaladık

    if (navbar.classList.contains("navbar-transparent")) {
        window.addEventListener("scroll", function () {
            if (window.scrollY > 50) {
                // SAYFA KAYDIRILDI (Menü Lacivert Oluyor)
                navbar.classList.remove("navbar-transparent");
                navbar.classList.add("navbar-scrolled");
                
                // Beyaz logoyu gizle, renkli logoyu göster
                logoWhite.classList.add("d-none");
                logoColor.classList.remove("d-none");
            } else {
                // EN TEPEYE DÖNÜLDÜ (Menü Şeffaf Oluyor)
                navbar.classList.add("navbar-transparent");
                navbar.classList.remove("navbar-scrolled");
                
                // Renkli logoyu gizle, beyaz logoyu göster
                logoColor.classList.add("d-none");
                logoWhite.classList.remove("d-none");
            }
        });
    } else {
        // EĞER SAYFA ANASAYFA DEĞİLSE (Hakkımızda vb. - Menü hep lacivert)
        // Başlangıçta direkt renkli logoyu göster
        if (logoWhite && logoColor) {
            logoWhite.classList.add("d-none");
            logoColor.classList.remove("d-none");
        }
    }
</script>
</body>
</html>